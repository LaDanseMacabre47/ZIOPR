stages:
  - build

build:
  stage: build
  tags:
    - bks2301
  script:
    - cmake --preset x64-debug
    - cmake --build out/build/x64-debug
  artifacts:
    paths:
      - out/build/x64-debug/bin/
    expire_in: 1 week
